#
# This file is the makefile for building CSIRX Recovery Test App for TI RTOS
#
ifeq ($(RULES_MAKE), )
include $(PDK_INSTALL_PATH)/ti/build/Rules.make
else
include $(RULES_MAKE)
endif

APP_NAME = csirx_recovery_testapp_$(BUILD_OS_TYPE)

include makefile.mk
