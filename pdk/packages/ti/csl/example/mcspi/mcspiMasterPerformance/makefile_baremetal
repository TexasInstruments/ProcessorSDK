#
# This file is the makefile for building mcspi performance test app for baremetal
#

ifeq ($(RULES_MAKE), )
include $(PDK_INSTALL_PATH)/ti/build/Rules.make
else
include $(RULES_MAKE)
endif

APP_NAME = csl_baremetal_mcspi_masterPerformance_app

BUILD_OS_TYPE=baremetal

include makefile.mk
