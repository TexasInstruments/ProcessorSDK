#
# This file is the makefile for building mcspi performance test app for TI RTOS
#

ifeq ($(RULES_MAKE), )
include $(PDK_INSTALL_PATH)/ti/build/Rules.make
else
include $(RULES_MAKE)
endif

APP_NAME = csl_mcspi_masterPerformance_app

include makefile.mk
